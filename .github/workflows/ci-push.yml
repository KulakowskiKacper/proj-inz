name: "[CI] Push"

defaults:
  run:
    shell: pwsh

on:
  push:
    branches: ["**"]     # run on push to ANY branch
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: placeholder
        run: Write-Host "placeholder"
